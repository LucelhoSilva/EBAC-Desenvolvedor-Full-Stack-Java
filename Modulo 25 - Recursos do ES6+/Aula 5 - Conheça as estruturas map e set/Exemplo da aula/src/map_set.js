let meuMap = new Map(); // Cria um novo Map

meuMap.set('nome', 'Lucelho'); // Adiciona um novo par chave-valor (novo item) ao Map

console.log(meuMap.get('nome')); // Exibe o valor associado à chave 'nome'